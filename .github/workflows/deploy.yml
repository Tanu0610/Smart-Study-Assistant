name: Deploy Link

on:
  push:
    branches:
      - main

permissions:
  deployments: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: Production
      url: https://smart-study-assistant-9qwd.onrender.com
    steps:
      - name: Register Deployment Link
        run: |
          echo "Successfully registered deployment environment."
          echo "URL: https://smart-study-assistant-9qwd.onrender.com"
